<form method="post" action="{{ path('app_unit_delete', {'id': unit.id}) }}" onsubmit="return confirm('Confirmer la suppression?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unit.id) }}">
    <button class="btn"><i class="bi bi-trash-fill me-2 icon"></i></button>
</form>
