<form method="post" action="{{ path('app_glass_delete', {'id': glass.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ glass.id) }}">
	<button class="btn">
		<i class="bi bi-trash-fill me-2 icon"></i>
	</button>
</form>
