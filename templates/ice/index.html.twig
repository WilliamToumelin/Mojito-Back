{% extends 'base.html.twig' %}

{% block body %}
	<div class="container-dynamic-block">

		<h1 class="display-6 container-dynamic-block-title">Les glaçons
			<small class=" container-dynamic-block-button-add">
				<a href="{{path('app_ice_new')}}">
					<i class="bi bi-file-plus"></i>
				</small>
			</a>
		</h1>

		<table class="table table-hover table-list ">
			<thead></thead>
			<thead>
				<tr>
					<th>Id</th>
					<th>Nom</th>
					<th>Modifier</th>
					<th>Supprimer</th>
				</tr>
			</thead>
			<tbody>
				{% for ice in ices %}
					<tr>
						<td>{{ ice.id }}</td>
						<td>{{ ice.name }}</td>
						<td>
							<a href="{{ path('app_ice_edit', {'id': ice.id}) }}">
								<i class="bi bi-pencil-square me-2 icon"></i>
							</a>
						</td>
						<td>
							{{ include('ice/_delete_form.html.twig') }}
						</td>
					</tr>
				{% else %}
					<tr>
						<td colspan="3">no records found</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}
